---
description: Automação gh/REST e scripts Node do spike (cursor-fixer e afins)
globs: javascript/**/*.js
alwaysApply: false
---

# Scripts JS (gh, Dependabot, Cursor)

Análise **humana** (fora do `cursor-fixer.js`) segue `rules/verify-issues-dependabot.md`: versão **universal** por pacote, olhando alertas abertos e issues/PRs existentes.

## GitHub CLI / REST

- Chamadas à API REST de Dependabot alerts: headers **`Accept: application/vnd.github+json`** e **`X-GitHub-Api-Version`** (ex.: `2022-11-28`), alinhado à doc oficial.
- Query string na URL (`state=open`, `per_page`, `page`) em vez de depender de `-f` em GET quando possível, para paridade com o workflow.

## Autenticação no processo Node

- Ordem: PAT → **`GH_REPO_TOKEN`** → **`GH_TOKEN`** → **`GITHUB_TOKEN`** (`tokenFromEnv` / `envWithGhCliAuth`); o `gh` no Actions exige **`GH_TOKEN`**, então qualquer token canônico é copiado para `GH_TOKEN` no `env` do `exec`/`execFileSync`.

## Estilo e mudanças

- **Um PR por pacote npm:** agrupar alertas pelo nome do pacote; versão-alvo = **maior** semver entre os patches da API (`maxPatchedVersionStrings`). Branch `security/fix-<pacote>` (sem id de alerta).
- **`createOrUpdateSecurityPR`:** após push, se já existir PR aberto com esse `head`, **`gh pr edit --body-file`** atualiza só a descrição (re-run do workflow).
- Reutilizar **`envWithGhCliAuth`**, **`pnpmExec`**, **`ghPrCreate`**, **`ghPrEditBody`**, **`gitPushAutomationBranch`**, **`formatSecurityPrBody`** (tabela multi-alertas + Cadeias + Grafo + Conclusão).
- Ao alterar `package.json`, respeitar **versões exatas** do projeto.
- Alinhar mensagens de erro a URLs canônicas (UI vs REST) quando o falhanço for por API/permissão.
